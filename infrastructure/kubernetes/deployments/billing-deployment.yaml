apiVersion: apps/v1
kind: Deployment
metadata:
  name: billing-service
  namespace: ai-scraping
spec:
  replicas: 2
  selector:
    matchLabels:
      app: billing-service
  template:
    metadata:
      labels:
        app: billing-service
    spec:
      containers:
      - name: billing-service
        image: ai-scraping/billing-service:latest
        ports:
        - containerPort: 8002
        envFrom:
        - secretRef:
            name: app-secrets
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"