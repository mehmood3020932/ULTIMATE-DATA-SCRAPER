apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: ai-scraping
spec:
  selector:
    app: notification-service
  ports:
  - port: 8004
    targetPort: 8004
  type: ClusterIP